// config.js

//Configure the IP/DNS and port for rest api

const config = {
    apiUrl: 'http://localhost:8080/api/v1',                    // Set the base URL for your API here
    port: 8080, 
};

export default config;
